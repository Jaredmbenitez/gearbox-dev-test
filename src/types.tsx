//Define type for Game including id, name, category, genre, price, discount, releaseDate, rating
type Game = {
    id: number;
    name: string;
    category: string;
    genre: string;
    price: number;
    discount: number;
    releaseDate: string;
    rating: number;
};

export default Game;
